import {
    setToastMessage,
    setIsOpen,
} from './toastMessageSlice';

import {
    setBreed,
    setPhotosDogs,
} from './dogsSlice';

import {
    setLang
} from './langSlice';

import rootSlice from './rootSlice';

export {
    setToastMessage,
    setIsOpen,
    setBreed,
    setPhotosDogs,
    setLang,
};

export default rootSlice;
