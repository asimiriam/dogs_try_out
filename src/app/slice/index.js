import {
    setToastMessage,
    setIsOpen,
} from './toastMessageSlice';

import {
    setBreed,
    setPhotosDogs,
} from './dogsSlice';

import rootSlice from './rootSlice';

export {
    setToastMessage,
    setIsOpen,
    setBreed,
    setPhotosDogs,
};

export default rootSlice;
